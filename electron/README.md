# Chess Electron UI

Electron UI wrapper for Chess Engine.

## Scripts

```sh
scripts/setup  # setup (PowerShell)
source scripts/setup.sh  # setup (bash)
npm run start  # run electron server with built react app
npm run react-start  # run react app dev server only
```